fastapi>=0.129.0
langchain>=1.2.10
langchain-community>=0.3.18
langchain-google-genai>=2.0.0
langchain-core>=0.3.0
langchain-text-splitters>=0.3.0
langgraph>=1.0.8
langsmith>=0.7.3
pydantic>=2.12.5
pydantic-settings>=2.6.0
pypdf>=6.7.0
python-dotenv>=1.2.1
requests>=2.32.5
sqlalchemy>=2.0.46
psycopg2-binary>=2.9.9
kafka-python>=2.0.2
boto3>=1.34.0
prometheus-client>=0.20.0
uvicorn>=0.40.0
yt-dlp>=2026.2.4

# NOTE: must be the -headless build. Plain opencv-python pulls in a GUI/X11
# dependency (libGL.so.1) that is absent on headless servers and most
# container base images, and `import cv2` will fail at runtime there.
opencv-python-headless>=4.8.0

google-genai>=0.1.0
sentence-transformers>=3.0.0
chromadb>=0.4.0
numpy>=1.26.0
scikit-learn>=1.3.0

# Only needed for backend/spark/train_transaction_model_spark.py -- the
# standalone batch retraining job, not the always-on web service. Installed
# here (unlike pyproject.toml's optional 'batch' extra) because
# requirements.txt has no extras mechanism; if you want the always-on API
# container to skip bundling PySpark + a JVM, install from pyproject.toml's
# base dependency set instead (`uv sync`, no --extra batch) rather than
# from this file.
pyspark>=3.5.0
pyarrow>=18.0.0

pytest>=8.0.0
moto>=5.0.0
